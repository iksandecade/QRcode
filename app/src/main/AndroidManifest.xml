<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.iksandecade.qrcode">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SelectActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TopupActivity" />
        <activity android:name=".BuyActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ShowQRCodeActivity" />
        <activity android:name=".PanitiaActivity" />
        <activity android:name=".ScanActivity" />
        <activity android:name=".SellerActivity" />
        <activity android:name=".PanitiaTopupActivity"></activity>
    </application>

</manifest>